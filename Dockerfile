# 1. Використовуємо офіційний образ Node.js
FROM node:18

# 2. Встановлюємо робочу директорію всередині контейнера
WORKDIR /app

# 3. Копіюємо package.json та встановлюємо залежності
COPY package.json .
RUN npm install

# 4. Копіюємо всі файли проекту у контейнер
COPY . .

# 5. Відкриваємо порт для серверу
EXPOSE 3000

# 6. Запускаємо програму
CMD ["node", "app.js"]